**CLI**
* ✔ <del>After 'run <filename>' the ANSI code is not changes, so the of the executable text appears in yellow.</del>

**Kernel**
* ✔ <del>'run' is running without full filename (e.g. 'Hello' runs 'HelloWorld')</del>
* ✔ <del>rename' is renaming without full filename (e.g. 'Hello' renames 'HelloWorld')</del>
* ✔ <del>'delete' is deleting without full filename (e.g. 'Hello' deletes 'HelloWorld')</del>
* ✔ <del>'chgattr' is changing without full filename (e.g. 'Hello' changes 'HelloWorld')</del>
* ✔ <del>'diskinfo' is not showing 'Bytes per Sector' nor 'Sectors per Block'</del>